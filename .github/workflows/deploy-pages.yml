name: Deploy protótipo no GitHub Pages

on:
  push:
    branches:
      - claude/health-app-security-module-gvca29
    paths:
      - prototype/**
      - .github/workflows/deploy-pages.yml
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configurar Pages
        uses: actions/configure-pages@v5

      - name: Publicar apenas o protótipo
        uses: actions/upload-pages-artifact@v3
        with:
          path: prototype

      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
